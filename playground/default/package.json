{
  "name": "default",
  "version": "0.0.0",
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open --port 5050",
    "now": "npm run build && npm run preview",
    "typecheck": "vue-tsc --noEmit"
  },
  "engines": {
    "node": ">= 20.0.0"
  },
  "devDependencies": {
    "iles": "^0.10.0-beta.1",
    "typescript": "^5.6",
    "vue-tsc": "^2.1"
  }
}
