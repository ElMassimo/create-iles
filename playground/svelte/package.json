{
  "name": "svelte",
  "version": "0.0.0",
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open --port 5050",
    "typecheck": "vue-tsc --noEmit"
  },
  "engines": {
    "node": "^14.18 || >= 16.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.9",
    "iles": "^0.8.6",
    "typescript": "^4.8.4",
    "vue-tsc": "^0.38.9"
  },
  "dependencies": {
    "svelte": "^3.52.0"
  }
}
