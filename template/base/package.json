{
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "vite preview --open --port 5050"
  },
  "devDependencies": {
    "iles": "^0.5.0",
    "vite": "^2.6.10"
  }
}
